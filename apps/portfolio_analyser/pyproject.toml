[project]
name = "portfolio-analyser"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "click",
    "quant_core",
    "quant_data",
    "quant-ui",
]

[tool.uv.sources]
quant_core = { workspace = true }
quant_data = { workspace = true }
quant-ui = { workspace = true }
