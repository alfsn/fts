# Linear Regression Hyperparameter Search Configuration File
study_name: "linear_regression_btc_optimization"
direction: "minimize"  # Minimize validation loss
n_trials: 1
model_type: "linear_regression"
market_id: "BTC_USD"
interval: "1h"

# Preprocessing & Feature parameters
lookback_period: 20
feature_cols: ["close"]

# Search Space Definitions (Deterministic model, empty search space)
search_space: {}
