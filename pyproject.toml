[project]
name = "fts-workspace"
version = "0.1.0"
description = "Workspace for the fts trading system"
authors = [
    { name = "Alfredo Sampron", email = "alfredosampronnoel@gmail.com" },
]
requires-python = ">=3.12"
readme = "README.md"
license = { text = "MIT" }
dependencies = [
    "pre-commit>=4.3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pre-commit",
    "black",
    "pycln",
    "isort",
    "flake8",
    "mypy",
]

[tool.uv.workspace]
members = [
    "src/trading_bot",
    "src/plugins/nets",
    "src/plugins/argentina",
    "src/plugins/yfinance",
    "src/plugins/ccxt",
]

[tool.black]
line-length = 88
target-version = ['py39']
include = '\.pyi?$'
exclude = '''
/(
    \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
)/
'''

[tool.isort]
profile = "black"

[tool.uv]
